
import { compareString } from 'ticket-system/helpers/compare-string';
import { module, test } from 'qunit';

module('Unit | Helper | compare string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = compareString([42]);
  assert.ok(result);
});

